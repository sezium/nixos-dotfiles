{
  programs.git = {
    enable = true;
    lfs.enable = true;
    settings = {
      user = {
        name = "sezium";
        email = "ssezium@proton.me";
      };
    };

  };

  programs.gh.enable = true;

}
